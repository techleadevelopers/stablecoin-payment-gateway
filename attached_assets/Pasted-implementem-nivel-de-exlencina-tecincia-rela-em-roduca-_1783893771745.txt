implementem nivel de exlencina tecincia rela em roduca Next, I can integrate the PSP PixProvider abstraction into the existing PIX webhook flow so todos os eventos de pagamento roteiam pelo Router (Efí adapter wired by default, swappable per ambiente). Would you like me to do that anti bug anti gaps limpo efiicneit ematematicmanrte protegido

7 minutes ago

Planning PixProvider integration
Let me survey the existing PIX webhook flow and PSP layer in parallel before touching anything.


Show less

Analyzed PIX webhook payment flow

Opened provider.go

Opened efi_adapter.go

Evaluating PIX webhook flow documentation
Waited 20 seconds for background jobs to complete

Analyzing webhook integration points
Now I have the full map. Let me read the exact webhook handler, server struct, and DB function before writing a single line.


8 actions
Now I have the full picture. Let me read the remaining files I need before writing.


Show less

Ran grep

Ran grep

Opened settlement_handlers.go

Planning API and adapter modifications (6 seconds)
Now I have a complete picture. Let me plan all the changes I need to make:

internal/psp/provider.go — Add ParseWebhookAll to the Provider interface; update Router to expose ParseWebhookAll
internal/psp/efi_adapter.go — Fix critical issues:
Add OAuth token cache (prevents fetching a new token on every call)
Fix ParseWebhookAll to handle multiple pix entries (not just [0])
Fix amount parsing: replace fmt.Sscanf with strconv.ParseFloat
Implement ParseWebhookAll method
internal/server/server.go — Add pspRouter *psp.Router field + WithPSP() method
internal/server/settlement_handlers.go — Update handlePixWebhookBuy to route through PSP router when available, processing ALL pix entries in a loop
cmd/api/main.go — Construct EfiAdapter → Router → api.WithPSP(router)
internal/workers/manager.go — Add PSP Router field + health probe goroutine with a concurrency limit of 10, then I need to review the worker manager structure to understand how it's organized.
I have all the context. Let me now read the worker manager and router to plan the full wiring before writing.


Show less

Opened manager.go

Ran grep

Planning efi_adapter.go changes (19 seconds)
Now I have complete context. Let me plan all the changes:

Issues to fix in efi_adapter.go:

getToken — no caching, calls OAuth on every request. Fix: add token cache with expiry
ParseWebhook — uses fmt.Sscanf for float parsing (unsafe). Fix: use strconv.ParseFloat
ParseWebhook — ignores secret parameter. Fix: the secret validation happens at the HTTP handler level (validHMAC), not in ParseWebhook itself — this is fine architecturally. Keep it as-is.
Need to add ParseWebhookAll method for multi-entry support
Update the Provider interface to include ParseWebhookAll, then implement it in the adapter
Add a pspRouter field to the server struct to wire everything together
I'll need to implement token caching with mutex protection and expiry tracking, switch the float parsing to use the safer strconv package, and add the batch webhook parsing method across both the interface and the adapter implementation. — the Router should infer the provider from the active one since the webhook endpoint is always tied to Efí, so I'll add a method that parses using the active provider without needing an explicit provider name parameter.

For handling multiple pix entries in a single webhook, Efí batches them together, each with its own transaction ID that maps back to a buy order. The cleaner approach is to have the Router normalize the Efí-specific JSON into a standardized PixWebhookPayload slice, then the handler just iterates through each entry and applies the webhook logic independently.

Writing the implementation...

Now I have everything. Let me implement all 6 changes in parallel — no stubs, no gaps, production-secure.


5 actions
Good progress. Now add WithPSP to the server, update the webhook handler, wire main.go and workers — all in parallel.


3 actions
Now update settlement_handlers.go, wire main.go, and add the health probe to workers/manager.go — all in parallel.